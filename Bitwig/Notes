LinVst may have some problems with some plugins and Bitwig.

Use the Makefiles with Bitwig in their name, Makefile-Bitwig for 64 bit vsts or Makefile-64-32bit-Bitwig for 64 bit and 32 bit vsts.

Choose (check) the plugins to be run Individually in the Bitwig Settings-Plug-ins panel.
